name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http: ^0.13.3
  localstorage: ^4.0.0+1
  utility_repository:
    path: ../utility_repository